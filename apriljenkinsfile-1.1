pipeline {
    agent any

    stages {
        stage('create to build application code') {
            steps {
                echo 'application code is build'
            }
        }
        stage('bulid') {
            steps {
                echo 'building is done'
            }
        }
        stage('deploy') {
            steps {
                echo 'deploying the application code'
            }
        }
    }
}
